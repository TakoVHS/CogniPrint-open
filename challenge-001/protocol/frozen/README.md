# Challenge 001 — frozen protocol area

Status: `EMPTY UNTIL FREEZE`

This namespace is reserved for the exact research-control artifacts used after `RESEARCH_FREEZE_001.md` changes from `PRE-FREEZE` to `FROZEN`.

Expected artifacts include, when genuinely frozen:

- a copy/reference of the frozen protocol state;
- Research Lock 001 manifest;
- exact software/evaluator commit identifiers;
- frozen metric/calibration/OOD/minimum-evidence definitions;
- frozen dataset/reference/split manifest hashes;
- external preregistration identifier/timestamp after submission.

## Do not pre-populate fake freeze artifacts

Placeholders are not evidence. This directory must not contain a file named as a final Research Lock, frozen corpus manifest or preregistration record until that artifact actually exists.

After preregistration, scientifically material changes are handled as explicit deviations/amendments or a new challenge. Do not overwrite the original frozen record.
