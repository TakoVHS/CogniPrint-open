name: RAID Pilot Adapter CI

on:
  push:
    branches: ["main"]
    paths:
      - ".github/workflows/raid-pilot-ci.yml"
      - "src/cogniprint/benchmarks/**"
      - "src/cogniprint/fingerprint.py"
      - "scripts/prepare_raid_pilot.py"
      - "scripts/analyze_raid_pilot.py"
      - "tests/test_raid_pilot_adapter.py"
      - "tests/test_raid_pilot_evaluation.py"
  pull_request:
    paths:
      - ".github/workflows/raid-pilot-ci.yml"
      - "src/cogniprint/benchmarks/**"
      - "src/cogniprint/fingerprint.py"
      - "scripts/prepare_raid_pilot.py"
      - "scripts/analyze_raid_pilot.py"
      - "tests/test_raid_pilot_adapter.py"
      - "tests/test_raid_pilot_evaluation.py"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  offline-adapter-test:
    name: Offline M1 tooling / Python ${{ matrix.python-version }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12"]

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install CogniPrint without external benchmark dependencies
        run: python -m pip install -e .

      - name: Compile M1 CLIs
        run: |
          python -m py_compile scripts/prepare_raid_pilot.py
          python -m py_compile scripts/analyze_raid_pilot.py

      - name: Run M1 offline unit tests
        run: |
          python -m unittest tests/test_raid_pilot_adapter.py -v
          python -m unittest tests/test_raid_pilot_evaluation.py -v
