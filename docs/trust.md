# CogniPrint Trust & Claim Status

Status date: 2026-07-26

This page is the canonical public trust summary for CogniPrint. It is not a marketing page and it does not expand scientific readiness beyond `descriptive_only`.

## Core rule

CogniPrint separates **capability** from **claim**.

A capability may compute a measurement or comparison. A public claim may be emitted only when the evidence and validation gates required by Claim Firewall are satisfied.

Example:

- capability: compute distance/similarity to a reference distribution;
- prohibited inference by default: “this artifact was generated by family X”.

Similarity is not identity.

## Truth classes

Every evidence record belongs to one of four classes:

- `OBSERVED` — directly measured by the reproducible analysis pipeline;
- `INFERRED` — a bounded statistical inference supported by an explicitly validated inference path;
- `ATTESTED` — supplied by an external provenance/credential source with its own validation state;
- `UNKNOWN` — evidence is insufficient, out of distribution, uncalibrated, conflicting, or the requested claim is unsupported.

CogniPrint does not collapse these classes into one confidence score.

## Claim Unlock Matrix

| Claim | Current status | What unlocks it |
| --- | --- | --- |
| Statistical profiling | `ALLOWED` | implemented descriptive measurement core |
| Profile similarity | `ALLOWED_WITH_BOUNDARY` | current comparison engine; must not be described as source identity |
| Generic human/AI separation | `LOCKED` | dedicated validated challenge evidence with scoped operating conditions |
| Model-family attribution | `LOCKED` | minimum-evidence rule + in-distribution gate + calibration + preregistered benchmark + acceptable false-known behavior |
| Exact-model identity from text | `LOCKED` | not unlocked by similarity; requires a separately validated method or external attestation appropriate to the claim |
| Human-intervention segmentation | `LOCKED` | dedicated controlled segmentation/revision-chain benchmark |
| Generation-lineage reconstruction | `LOCKED` | dedicated multi-stage lineage experiments after lower-level gates succeed |
| Provenance interpretation | `PARTIAL / CONTRACT ONLY` | validated C2PA/provider/log/revision adapters and conflict tests; issue #26 tracks C2PA runtime |
| Cryptographically signed `.cogcase` | `LOCKED` | reviewed detached-signature implementation and verification; issue #27 |
| Actor/commissioner identity | `LOCKED` | independent authenticated evidence appropriate to the claim; never inferred from prose alone |
| Legal/compliance conclusion | `LOCKED` | external legal assessment for the exact deployment/use case; technical evidence alone is insufficient |

A claim remains locked even if a prototype can technically produce a score.

## Scientific readiness

Current scientific readiness: `descriptive_only`.

Research mode: `PROOF_MODE`.

Challenge 001 Stage B: `NOT_AUTHORISED_TO_START`.

This means CogniPrint currently supports reproducible measurement and evidence structuring. It does not currently have validated source-attribution authority.

## Current execution evidence status

### GitHub Actions

`NOT_EXECUTED`

A minimal Runner Canary was created with no checkout, dependencies, cache, matrix or secrets. The job was created, but GitHub reported `steps: []`; neither the shell canary nor `python --version` executed. A control rerun after GitHub reported Actions operational produced the same outcome.

Therefore the project must not describe those runs as:

- tests failed;
- tests passed;
- partially validated.

The correct statement is:

> CI execution: NOT_EXECUTED — runner did not reach the first executable step.

### Local exact-snapshot execution

`NOT_EXECUTED`

The current local execution environment could not resolve/fetch `github.com`, so it could not obtain an exact repository snapshot for the requested Python 3.10/3.11/3.12 execution matrix. This is not a local PASS or FAIL.

Issue #30 closes only after the trust/evaluator tests actually execute on a functioning runner and the runner/Python versions, commit SHA, job logs and test counts are preserved.

## Attribution Challenge 001 — pre-freeze state

Stage B status: `NOT AUTHORISED TO START`.

The project now separates:

```text
Stage A development
  -> leakage audit
  -> protocol freeze
  -> Research Lock
  -> external preregistration
  -> sealed Stage B
  -> frozen predictions
  -> label reveal
  -> independent evaluation
```

### Stage A

Stage A is development-only. It exists to fix numerical and operational choices before freeze, including minimum-evidence/length bins, OOD/UNKNOWN methodology, calibration procedure, feature stability, sample feasibility, exclusions and evaluator sanity.

Anything used to make those choices is development-visible and cannot later be represented as sealed Stage B evidence.

The Development Exposure Registry already quarantines:

- CogniPrint `public-benchmark-v1.1`;
- the already specified RAID `raid/train` Pilot A matrix.

A Stage A public-benchmark materializer exists, but a real materialized Stage A manifest has **not yet been executed/preserved** because the available runners remain unavailable.

### Stage A / Stage B boundary

The blinded-sample schema enforces:

- Stage A: `development_visibility=true`, `evaluation_visibility=false`;
- sealed Stage B: `development_visibility=false`, `evaluation_visibility=true`, `reference_set_membership=SEALED_EVALUATION`.

Direct ground-truth fields such as `true_class`, `known_to_reference`, generator/model-family labels and `ground_truth` are prohibited from the blinded Stage B record format.

Ground truth lives in a separate revealed-label schema and is exposed only after predictions are frozen and hashed.

### Leakage gate

Before freeze, real Stage A and candidate Stage B manifests must pass the machine-readable leakage audit.

Minimum freeze conditions include:

```text
sample_id_overlap = 0
content_hash_overlap = 0
```

Duplicate sample/content identifiers inside a stage also block the gate. Prompt-hash overlap is reported separately and its policy must be fixed in the frozen protocol.

No real A/B leakage report exists yet.

### Research Lock 001

Research Lock 001 tooling is implemented to bind selected frozen protocol/evaluator/config/manifest/reference files plus the exact repository commit into one deterministic SHA-256.

It is an integrity binding only. It is **not**:

- a digital signature;
- a signer-identity proof;
- a scientific-validity certificate.

The final lock file set and final Research Lock hash do not exist yet because the protocol remains `PRE-FREEZE`.

## Evidence dimensions are not one confidence number

CogniPrint keeps at least these concepts separate:

- **reference similarity** — how close an artifact is to a stated reference distribution;
- **calibration status/quality** — whether an inference score has demonstrated probability reliability on the relevant validation scope;
- **provenance/attestation strength** — what external credential/log/metadata evidence exists and how it validated.

These must never be magically combined into a headline such as “93% AI”.

## Integrity and signatures

Current `.cogcase` state:

- deterministic manifest: implemented;
- SHA-256 file-set integrity: implemented;
- tamper/missing/unexpected-file checks: implemented;
- symlink and duplicate-manifest-path rejection: implemented;
- digital signature: **NOT IMPLEMENTED**;
- accepted signature status in v0.1: `UNSIGNED` only.

Current safe wording:

> integrity-verifiable / tamper-detecting case manifest

Prohibited until issue #27 closes:

> cryptographically signed CogniPrint case

## External provenance

C2PA / Content Credentials support is currently an interface contract, not a production runtime capability.

Issue #26 requires known-valid, tampered/invalid, absent and unsupported fixtures plus trust-configuration and privacy review before runtime support may be claimed.

CogniPrint treats external provenance as `ATTESTED` evidence. It does not treat any one external signal as absolute truth and preserves conflicts instead of silently resolving them.

## Current failures / limitations

Machine-readable limitations already include states such as:

- `DESCRIPTIVE_ONLY`;
- `INSUFFICIENT_EVIDENCE`;
- `SHORT_TEXT`;
- `OUT_OF_DISTRIBUTION`;
- `UNSEEN_DOMAIN`;
- `LOW_REFERENCE_COVERAGE`;
- `UNCALIBRATED_SCORE`;
- `NO_EXTERNAL_PROVENANCE`;
- `CONFLICTING_PROVENANCE`;
- `UNSUPPORTED_CLAIM`.

The public failure charter is `docs/where-cogniprint-fails.md`.

## High-stakes boundary

CogniPrint output must not by itself be used to:

- accuse a person of misconduct;
- automatically punish a student;
- fire or discipline an employee;
- determine legal responsibility;
- identify an operator/commissioner from content alone.

A technical evidence report does not replace domain-specific human review, due process, authenticated external evidence, or legal assessment.

## No-new-feature rule until Challenge 001

Until Evidence Milestone 001 is reached, work that changes project scope is frozen.

Allowed work:

1. correctness/security bug fixes;
2. reproducibility;
3. test/runner infrastructure;
4. Stage A development calibration required to freeze the protocol;
5. preregistration/freeze work;
6. benchmark integrity/sealing;
7. corrections required to keep public claims aligned with evidence.

Deferred work:

- new detector families;
- new dashboards or decorative product layers;
- new regulatory integrations beyond maintaining already-documented mappings;
- additional provenance adapters not required for Challenge 001;
- new headline capabilities.

## Evidence Milestone 001

The milestone is achieved only when all of the following exist together:

- timestamped preregistration;
- actually executed reproducible test suite;
- frozen/sealed blind benchmark;
- frozen prediction artifact and SHA-256;
- post-freeze revealed-label artifact and SHA-256;
- independent evaluator output;
- calibration metrics where calibrated outputs are claimed;
- OOD/UNKNOWN metrics including false-known rate;
- published failure analysis;
- immutable integrity-verifiable evidence bundle;
- external methodological review requested against the actual results.

Reaching this milestone does **not** automatically unlock model-family attribution. A separate Scientific Claim Review must assign each proposed claim one of:

- `ALLOW`;
- `ALLOW_WITH_SCOPE`;
- `REJECT`;
- `REQUIRES_MORE_EVIDENCE`.

The goal of the milestone is not to manufacture a positive result. It is to create a result whose scope cannot be made prettier after the data are known.
